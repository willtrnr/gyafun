;; First test program "count" arguments

io.println: native

main:

  $argc 0
  $count 1

  ST0 $count

  .loop:
    LDL $argc
    IZR .done

    LDL $count
    LD1
    ADD
    STL $count

    LDL $argc
    LD1
    SUB
    STL $argc

    JMP .loop
  .done:

  LDL $count
  IVK io.println 1
